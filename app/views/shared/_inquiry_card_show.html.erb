All info.  Buyer show.  Request to join if current user is a seller.  Instructions hidden.

Put it in here and elsewhere!
<%= link_to 'like', inquiry_likes_path(@inquiry), class: 'btn btn-primary' %>
    <%= link_to 'unlike', inquiry_unlikes_path(@inquiry), class: 'btn btn-primary' %>
