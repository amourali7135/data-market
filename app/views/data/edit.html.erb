Do it right...

<div class="container mt-3">
<div class="row align-items-center justify-content-center">
<h2>Please provide the info for whatever you feel comfortable sharing with others!</h2>
</div>

<div class="row align-items-center justify-content-center">
<%= simple_form_for [@seller, @data] do |f|%>
<%= f.input :format %>
<%= f.input :content %>
<%= f.input :types, collection: Datum.types, input_html: {multiple: true, id: "data_type_new"}, label: "What type of data is this?" %>
<div style="text-align:center">
<%= f.button :submit, 'Submit', class: 'btn btn-primary'%>
</div>
<% end %>
</div>
</div>
